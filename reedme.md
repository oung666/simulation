# 项目修改记录

## 6.16 修改时间

### 1. 仿真时钟与播放控制

- 新增独立仿真时钟 `simulation/core/clock.py`，把仿真时间推进从界面刷新频率中拆出来。
- 当前时钟统一负责 `播放 / 暂停 / 复位 / 单步推进 / 倍率切换`，`MapCanvas` 不再自己零散管理时间。
- 修正了“点击开始不动、点击步进后才动、切回 1x 又不动”的问题，现在：
  - `开始` 会真正启动时钟推进。
  - `单步` 会精确推进 1 个仿真秒。
  - `1x / 2x / 4x / 8x / 100x` 倍率都会通过时钟压缩比生效。
- 顶部工具栏中的播放按钮、步进按钮、倍率下拉框，已经统一接到时钟逻辑。

### 2. 地图显示与交互

- 恢复左侧 `显示控制` 面板，重新接回：
  - `显示雷达范围圈`
  - `显示雷达动态效果`
  - `显示通信态势共享`
- 保持界面精简，不恢复旧版独立图层管理面板，图层拆分与显隐逻辑继续在代码内部统一管理。
- 地图交互已调整为：
  - 左键选中单位
  - 右键查看单位信息
- 地图下方和右侧已经加入滚动条，用于视口平移控制。
- 滚轮缩放逻辑已改为尽量以鼠标所在位置为缩放中心；当没有更高等级瓦片时，也继续对当前图片做放大，而不是强行跳转到高等级瓦片中心。

### 3. 任务查看与战斗单元

- `任务查看` 已并入 `战斗单元` 窗口，不再单独占一个面板。
- 当前 `战斗单元` 窗口为分页结构，可只读查看：
  - 单位列表
  - 任务列表
  - 任务参与单位
  - 当前任务关联目标及目标信息
- 界面中原来不必要的独立任务面板、图层管理面板、路线管理面板都已移除。

### 4. 场景与 JSON 结构调整

- 场景结构已调整为 `scene.json + units/*.json`。
- 每个战斗单元单独一个 JSON，不再把所有单位混放在一个主场景文件里。
- `scene.json` 负责地图、时间压缩、单位索引、任务索引等主场景装配信息。
- `units/*.json` 负责单位自身属性、位置、阵营、路线引用、武器和探测参数等。
- 目前场景加载器兼容两种格式：
  - 旧格式：主文件内联 `units`
  - 新格式：主文件通过 `unit_files` 引用单位文件

### 5. 单位数据扩展

- 单位数据已补充 `altitude_ft` 字段，用于空中单位高度表达。
- 单位信息弹窗已支持显示高度相关信息。
- 后续可以继续按这个结构扩展：
  - 挂载
  - 传感器状态
  - 油量
  - 弹药
  - 编队/隶属关系

### 6. 航线与图层拆分推进

- 已清理未使用的固定航线，仅保留当前被战斗单元实际引用的航线。
- 战斗单元正在使用的航线目前不会被误删。
- 右键自动绘制航线入口先不开放，但动态绘制相关代码仍然保留，方便后续恢复。
- 目前已经开始朝“单位层 / 航线层 / 范围圈层 / 标注层”拆分的方向推进，但 `MapCanvas` 仍有进一步拆分空间。

## 当前 JSON 组织方式

- `scene.json`：主场景入口，负责地图、单位清单、任务清单、时间参数等总装配信息。
- `units/*.json`：每个战斗单元一个文件，负责单位本体属性与状态定义。
- `routes.json`：统一管理固定航线，供需要的战斗单元引用。

## 后续建议

### 一、下一阶段最值得先做的 4 件事

1. 先补 `阵营关系 + 条令体系`

- 你现在自动交战还是按 `side != unit.side` 直接判断敌我，这在演示阶段够用，但一旦要做更像样的推演，这会很快卡住。
- `panopticon-main` 已经有 `Relationships.py` 和 `Doctrine.py` 这两层骨架：
  - `Relationships` 负责敌对 / 友军关系
  - `Doctrine` 负责阵营级行为规则，比如是否主动攻击、是否追击、是否返航
- 你这边最适合先做成：
  - `Side` / `Relationship` 数据结构
  - `Doctrine` 数据结构
  - `CombatController` 的交战判断改为“关系 + 条令”联合判定
- 这是最关键的一步，因为后面的任务、返航、自动攻击、编队协同都依赖它。

2. 把 `任务系统` 从“可显示”推进到“可执行、可阶段化”

- 你现在已经有 `PatrolMission` 和 `StrikeMission`，这是很好的起点。
- 但相对 `panopticon-main` 和更完整的推演系统，还缺“任务生命周期”：
  - 任务准备
  - 出航 / 起飞
  - 集结
  - 巡逻 / 突击
  - 交战
  - 返航 / 回收
  - 完成 / 失败
- 建议下一步给任务补：
  - `mission_phase`
  - `start_condition`
  - `end_condition`
  - `rtb_condition`
  - `priority`
- 这样你的任务就不只是“给几个单位和目标”，而是开始具备真正的指挥控制逻辑。

3. 把 `保障与持续作战` 做起来

- 你当前模型里已经有 `current_fuel / max_fuel / fuel_rate`，说明数据层准备得不错，但逻辑上还没有真正驱动作战行为。
- 相对军事仿真游戏或墨子这类系统，这一块是明显缺口。
- 适合先补的不是复杂补给网，而是最小闭环：
  - 飞机按时间消耗燃油
  - 低油量触发返航
  - 空军单位绑定 `home_base`
  - 机场 / 舰艇可作为回收节点
  - 弹药耗尽也触发返航或退出任务
- 这一步做完以后，系统会从“地图动画”更像“作战推演”。

4. 建立 `回放 / 记录 / 复盘`

- `panopticon-main` 已经有 `PlaybackRecorder.py`，会按时间片记录仿真过程。
- 你现在有独立时钟，已经非常适合接回放系统。
- 建议下一步做：
  - 每 N 秒记录一次场景快照
  - 记录单位位置、存活状态、任务状态、武器状态、探测态势
  - 支持回放拖动和指定时刻重看
- 这不只是“好看”，而是后续调试 AI、验证战术逻辑、分析推演结果的基础设施。

### 二、对比 panopticon-main，目前你还明显缺的骨架

1. `Side` 层级不完整

- `panopticon-main` 有明确的 `Side.py`，阵营不是简单的 `blue/red` 字符串，而是正式对象。
- 你现在更像“单位直接挂 side 字段”，短期够用，但后面很难承载：
  - 阵营颜色
  - 阵营得分
  - 阵营关系
  - 阵营条令
  - 阵营资源限制

2. `Relationship + Doctrine` 尚未进入主逻辑

- 这是当前和 `panopticon-main` 最核心的差距。
- 你自己的文档里也已经明确写了，目前交战仍按 `side != unit.side` 判断。
- 这个不补上，后续很多仿真行为都会显得“太硬编码”。

3. `任务编辑 / 任务阶段 / 任务触发器` 不够

- 现在的任务更像“静态任务配置”。
- 更完整的系统需要：
  - 任务启停
  - 任务切换
  - 任务优先级
  - 任务触发器
  - 目标丢失后的替代逻辑
  - 执行失败后的后备方案

4. `基地、舰载机、出动回收` 机制不够

- `panopticon-main` 里 `Game.py` 有从舰艇 / 机场放飞飞机、回收飞机等逻辑。
- 你现在虽然有 `airbase` 类型，但还没有形成“单位储备 - 起飞 - 执行 - 回收”的完整链路。
- 对空战和持续推演来说，这块非常重要。

5. `单位数据库` 还可以继续正规化

- 你已经有 `core/db`，这一点其实不错。
- 但要继续往更像 `panopticon-main` 的 `UnitDb` 方向推进：
  - 单位平台参数与场景实例参数彻底分离
  - 武器库独立
  - 传感器库独立
  - 挂载方案独立
  - 不同国家 / 阵营的平台模板独立
- 这样后面批量扩场景和做想定会轻松很多。

### 三、如果对标“墨子”这类更完整的军事仿真系统，还缺什么

如果把你现在的系统看作“轻量战术推演原型”，那相对更完整的军事仿真平台，通常还会缺下面几层：

1. 指挥控制层

- 条令
- 交战规则
- 阵营关系
- 编队 / 编组
- 任务树
- 触发器与事件链

2. 兵力运用层

- 起飞 / 出航 / 回收
- 编队协同
- 伴随掩护
- 护航 / 截击 / 压制 / 佯动
- 多批次任务组织

3. 传感器与电磁层

- 主动 / 被动探测区分
- 多传感器融合
- 电子干扰 / 抗干扰
- 数据链质量退化
- 通联中断后的态势衰减

4. 武器与交战层

- 更细的武器发射条件
- 多目标分配
- 超射 / 协同拦截
- 武器中段制导 / 末段制导差异
- 防空反导与饱和攻击逻辑

5. 保障与资源层

- 燃油消耗
- 弹药消耗
- 机场 / 港口容量
- 维护与再出动时间
- 补给与持续作战能力

6. 评估与推演分析层

- 任务完成度
- 战损统计
- 阵营得分
- 关键事件时间线
- 回放复盘
- 多方案对比

### 四、结合你当前工程，建议的推进顺序

建议不要一下子追求“像墨子那样全”，而是按下面顺序推进：

1. 第一阶段：把“规则链”补齐

- `Side`
- `Relationship`
- `Doctrine`
- `MissionPhase`
- `Trigger / Event`

这一阶段完成后，仿真行为会先从“写死”变成“可配置”。

2. 第二阶段：把“作战闭环”补齐

- 燃油消耗
- 弹药消耗
- 返航逻辑
- 起飞 / 回收
- 基地 / 母舰保障

这一阶段完成后，系统会真正具备持续作战能力。

3. 第三阶段：把“复盘分析”补齐

- 录制
- 回放
- 战损统计
- 任务完成率
- 阵营得分

这一阶段完成后，你的系统就不只是能跑，还能评估和比较。

4. 第四阶段：把“界面编辑能力”补齐

- 任务编辑器
- 阵营关系编辑器
- 条令编辑器
- 触发器编辑器
- 编队 / 挂载编辑器

这一阶段才适合做更完整的想定制作工具。

### 五、最现实的结论

- 你现在这套代码，已经不是单纯的地图演示了，已经有了轻量仿真内核的雏形。
- 相对 `panopticon-main`，你最该先补的是：`关系 + 条令 + 任务阶段 + 保障闭环 + 回放`。
- 相对“墨子”这类完整系统，真正差距不在画面，而在“指挥控制逻辑、持续作战逻辑、评估复盘逻辑”。
- 所以后续不要优先堆更多零散 UI，而是优先把这几条主链做扎实。
